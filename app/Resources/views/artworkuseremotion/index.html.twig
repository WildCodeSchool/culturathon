{% extends 'base.html.twig' %}

{% block body %}
    <h1>Artworkuseremotions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artworkUserEmotion in artworkUserEmotions %}
            <tr>
                <td><a href="{{ path('artworkuseremotion_show', { 'id': artworkUserEmotion.id }) }}">{{ artworkUserEmotion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('artworkuseremotion_show', { 'id': artworkUserEmotion.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('artworkuseremotion_edit', { 'id': artworkUserEmotion.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artworkuseremotion_new') }}">Create a new artworkUserEmotion</a>
        </li>
    </ul>
{% endblock %}
