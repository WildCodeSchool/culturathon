{% extends 'base.html.twig' %}

{% block body %}
    <h1>Artworkuseremotion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artworkUserEmotion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artworkuseremotion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('artworkuseremotion_edit', { 'id': artworkUserEmotion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
