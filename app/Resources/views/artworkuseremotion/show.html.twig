{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Artworkuseremotion</h1>

        <table class="table table-bordered">
            <thead class="thead-dark">
            <tr>
                <th>Id</th>

            </tr>
            </thead>
            <tbody>
            <tr>

                <td>{{ artworkUserEmotion.id }}</td>
            </tr>
            </tbody>
        </table>
        <a href="{{ path('artworkuseremotion_index') }}" class="btn btn-info">Back to the list</a>

        <a href="{{ path('artworkuseremotion_edit', { 'id': artworkUserEmotion.id }) }}" class="btn btn-info">Edit</a>

        {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
