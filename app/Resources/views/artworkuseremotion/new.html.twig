{% extends 'base.html.twig' %}

{% block body %}
    <div class="row my-5">
        <div class="offset-3 col-md-6 mx-auto shadow">
            <h1 class="text-center">Artworkuseremotion creation</h1>
            <div class="container">
                <div class="row">
                    <div class="d-flex">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <input type="submit" value="Create"/>
                        {{ form_end(form) }}

                        <div class="d-flex">
                            <a href="{{ path('artworkuseremotion_index') }}">Back to the list</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
