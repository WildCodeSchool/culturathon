{% extends 'base.html.twig' %}

{% block body %}
    <div class="row my-5">
        <div class="offset-3 col-md-6 mx-auto shadow">
            <h1 class="text-center">Museum creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" class="btn btn-info"/>
            {{ form_end(form) }}

            <a href="{{ path('museum_index') }}" class="btn btn-info mt-2">Back to the list</a>

        </div>
    </div>
{% endblock %}
